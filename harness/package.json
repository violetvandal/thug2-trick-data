{
  "name": "thug2-trick-data-harness",
  "private": true,
  "description": "Local checks for the dataset. Run `npm install` here, then `npm test`.",
  "scripts": {
    "test": "node test.js && node xcheck.js"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  }
}
